Experimental save file


import json
from pprint import pprint
with open("spaghetti", "r") as reading:
  try:
    saves = json.loads(reading.read())
  except json.decoder.JSONDecodeError as err:
    print ("oh no this bad")
    saves = []

saves.append({
  'player': 'Barth',
  'xp': 1274,
})
saves.append({
  'player': 'Leonard',
  'xp': 3456,
})

with open("spaghetti", "w") as output:
  output.write(json.dumps(saves))

pprint (saves)
pprint(saves[0])
print(saves[1]['player'])
